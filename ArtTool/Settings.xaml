<Window x:Class="ArtTool.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArtTool"
        mc:Ignorable="d"
        Title="Settings" Height="581" Width="627"
        ResizeMode="CanResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#202020">
    
    <WindowChrome.WindowChrome>
        <WindowChrome 
        CaptionHeight="30"
        ResizeBorderThickness="5" />
    </WindowChrome.WindowChrome>
    <Grid x:Name="MainGrid">
        <Grid VerticalAlignment="Top" Background="#151515">
            <StackPanel Height="30" WindowChrome.IsHitTestVisibleInChrome="True" VerticalAlignment="Center" HorizontalAlignment="Right" Orientation="Horizontal">
                <Button x:Name="MaximizeButton" Content="+" Height="25" Width="25" Margin="2" Click="MaximizeButton_Click"/>
                <Button x:Name="CloseButton" Content="x" Height="25" Width="25" Margin="2" Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>
        <Grid Name="SettingsWindow" Margin="0,30,0,20" Background="#101010">
        </Grid>

    </Grid>
</Window>